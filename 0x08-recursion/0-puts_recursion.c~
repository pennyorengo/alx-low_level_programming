#include "main.h"

/**
 * _puts_recursion - prints a string followed by a new line
 * Return: Always 0
 */

void _puts_recursion(char *s)
{
	char *s
	_putchar("%s Puts with recursion *s\n");
	
	_putchar(*s);

}

	return (0);
}
